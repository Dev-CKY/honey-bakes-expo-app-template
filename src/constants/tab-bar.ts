import { moderateScale } from "react-native-size-matters";

export const TAB_BAR_COLORS = {
  background: "#F6F0D4",
  active: "#F7BC5D",
};

export const TAB_BAR_SIZES = {
  height: moderateScale(78),

  activeIconContainer: moderateScale(58),
  inactiveIconContainer: moderateScale(48),

  icon: moderateScale(24),
};
